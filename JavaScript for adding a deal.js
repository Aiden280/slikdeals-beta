document.querySelector('#add-deal form').addEventListener('submit', (event) => {
  event.preventDefault(); // Prevent form submission
  // Add the deal to the database using AJAX or server-side scripting
});
